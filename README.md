# TECHZONE Website

Open `index.html` to view the storefront. The account menu links to `login.html` and `signup.html`.

The included account flow is a browser-only demo that stores hashed credentials and session data in `localStorage`. Connect the forms to a secure server-side authentication service before deploying a production website.
